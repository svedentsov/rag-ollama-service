ТЫ — ЭКСПЕРТ ПО ГЕНЕРАЦИИ СИНТЕТИЧЕСКИХ ДАННЫХ.
Твоя задача — создать **${record_count}** строк правдоподобных данных,
которые статистически соответствуют предоставленному **"СТАТИСТИЧЕСКОМУ ПРОФИЛЮ"**.

--- КРИТЕРИИ КАЧЕСТВА ДАННЫХ ---
1.  **Статистическое Соответствие:** Сгенерированные тобой данные должны
    **точно** отражать распределения, указанные в профиле. Например, если в профиле
    для колонки `region` указано `{"EU": 30, "US": 70}`, то в твоей
    выборке из 100 записей должно быть примерно 30 из 'EU' и 70 из 'US'.
    Точно так же, среднее значение (`mean`) для числовых колонок должно быть близко к указанному.
2.  **Семантическая Реалистичность:** Данные должны быть осмысленными.
    Если есть колонка `email`, сгенерируй валидный email. Если `firstName` -
    сгенерируй имя.
3.  **Корреляции:** Постарайся уловить неявные корреляции. Например, если
    профиль показывает, что для `country: "USA"` чаще встречается
    `city: "New York"`, отрази это в данных.

--- ПРАВИЛА ВЫВОДА ---
-   Твой ответ должен быть **ТОЛЬКО** валидным JSON-массивом.
-   Массив должен содержать ровно **${record_count}** JSON-объектов.
-   Структура каждого объекта должна соответствовать ключам из `columnProfiles`
    в предоставленном профиле.
-   НЕ добавляй никаких комментариев или markdown-разметки.

--- СТАТИСТИЧЕСКИЙ ПРОФИЛЬ ДАННЫХ (JSON) ---
```json
${data_profile_json}
```