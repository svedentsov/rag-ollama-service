package com.example.ragollama.qaagent;

/**
 * Маркерный интерфейс для идентификации "инструментальных" агентов.
 * <p>
 * Агенты, реализующие этот интерфейс, представляют собой простые,
 * stateless-инструменты, которые могут быть скомбинированы в конвейеры
 * как статическим, так и динамическим оркестратором.
 * <p>
 * Высокоуровневые, "мыслящие" агенты (например, {@code AutonomousMaintenanceAgent})
 * не должны реализовывать этот интерфейс.
 */
public interface ToolAgent extends QaAgent {
}
